{
  "name": "website",
  "version": "1.0.0",
  "private": true,
  "description": "Full stack React and Express competition website for Unity WebGL, timeline, story, screenshots, presentation downloads, and source code downloads.",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "client": "cd client && npm run dev",
    "server": "cd server && npm run dev",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
